<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp">

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:maxHeight="3dip"
        android:minHeight="3dip"
        android:progressDrawable="@drawable/custom_progressbar"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/nameView"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/progressBar"
        android:layout_marginLeft="12dp" />

    <TextView
        android:id="@+id/purchaseDateView"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/nameView"
        android:layout_marginLeft="12dp" />

    <TextView
        android:id="@+id/visitsNumberView"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:layout_alignParentRight="true"
        android:layout_below="@id/progressBar"
        android:layout_marginRight="12dp" />

    <Button
        android:id="@+id/showVisitsButton"
        android:width="12dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/purchaseDateView"
        android:layout_marginTop="20dp"
        android:text="@string/show_history" />

    <Button
        android:id="@+id/startNewVisitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start_visit"
        android:layout_alignBottom="@+id/showVisitsButton" />

</RelativeLayout>